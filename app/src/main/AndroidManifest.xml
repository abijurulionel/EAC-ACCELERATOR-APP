<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abidjalionel.massguest">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.NETWORK" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MassGuest">
        <activity android:name=".firstpagee"></activity>
        <activity android:name=".ui.video.VideoActivity" />
        <activity android:name=".ui.findus.FindusActivity" />
        <activity
            android:name=".welcomehome"
            android:label="@string/title_activity_welcomehome"
            android:theme="@style/Theme.MassGuest.NoActionBar" />
        <activity android:name=".passwordresetactivity" />
        <activity android:name=".registrationactivity" />
        <activity android:name=".homeactivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>